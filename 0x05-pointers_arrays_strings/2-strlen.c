#include "main.h"
/**
 * _strlen- returning the lengt of the string
 * @ s:
 * Return: int
 */
int _strlen(char *s)
{
	int arr[i], int count;


	for (i=0; *(arr+i) != '\0'; i++)
	{
		count= *(arr+i)
			_putchar(count);
	}
	return (0);
}

