#include "main.h"
/**
 * _strlen- returning the lengt of the string
 * @ s:
 * Return: int
 */
int _strlen(char *s)
{
	  int i; int arr[i];


	for (i=0; *(arr+i) != '\0'; i++)
	{
		s= *(arr+i);
			putchar(s);
	}
	return (0);
}

