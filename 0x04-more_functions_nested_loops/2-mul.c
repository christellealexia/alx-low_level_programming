#include "main.h"
/**
 * int mul- multiplying two integers
 * @ a: integer a to be multiplied with b
 * @ b: integer b to be multiplied with a
 */
int mul(int a, int b)
{
	int d;
	 d= a* b;
	 return d;
}
