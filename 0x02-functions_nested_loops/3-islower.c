#include <stdio.h>

/**
 * main -Entry point
 * Description: 'if alphabet is lowecase'
 * Return: int
 */
int _islower(int c)
{
	if( c >= 97 && c <= 122)
	{
	
	return (1);
	}
	else
	{
	
	return (0);
	}
}
